# Konfiguracja Prometheusa: co 5 s pobiera metryki z API.
global:
  scrape_interval: 5s

scrape_configs:
  - job_name: "hotel-cancellation-api"
    metrics_path: /metrics
    static_configs:
      - targets: ["api:8000"]
