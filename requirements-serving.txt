# Minimalne zależności obrazu serwującego (Docker).
# Wersje pandas/numpy/scikit-learn MUSZĄ być zgodne ze środowiskiem
# treningowym — inaczej wczytanie model.pkl może się nie powieść.
fastapi==0.136.3
uvicorn==0.49.0
prometheus-client==0.25.0
pandas==2.3.3
numpy==2.4.4
scikit-learn==1.8.0
