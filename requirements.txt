# === Środowisko projektu (wersje przypięte dla odtwarzalności) ===

# Analiza danych i notebook (etap: baseline)
pandas==2.3.3
numpy==2.4.4
scikit-learn==1.8.0
matplotlib==3.10.8
seaborn==0.13.2
jupyter

# Pipeline ML (etap: Kedro) + śledzenie eksperymentów (MLflow)
kedro==1.4.0
kedro-datasets[pandas]==9.4.0
kedro-viz
pyarrow==24.0.0
mlflow==3.13.0

# Jakość kodu i testy (CI)
ruff==0.15.16
pytest==9.0.3

# === Udoskonalanie modelu ===
optuna==4.2.1                        # strojenie hiperparametrów (bayesowskie)
xgboost==3.0.0                       # porównanie modeli

# Wersjonowanie danych (etap: MLOps)
dvc==3.67.1

# Serwowanie modelu + monitoring (etap: produkcja)
fastapi==0.136.3
uvicorn==0.49.0
prometheus-client==0.25.0

# === Dodawane w kolejnych etapach ===
# autogluon                            # AutoML (uruchamiane w Colab)
